# .dev.vars.example
#
# Used by `wrangler dev` for local Cloudflare Worker secret injection.
# Copy to .dev.vars and fill in real values.
# .dev.vars is gitignored — NEVER commit real values here.
#
#   cp .dev.vars.example .dev.vars

# ── Cloudflare Worker Secrets ────────────────────────────────────────
# https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key

# Interim shared client token required by protected Worker POST routes.
# Generate a long random value and use the same value locally in:
#   .dev.vars                 -> BIP_CLIENT_TOKEN
#   .env.local                -> EXPO_PUBLIC_BIP_CLIENT_TOKEN
# The EXPO_PUBLIC copy is extractable from the app bundle. It is a speed bump,
# not permanent identity. Supabase JWT verification is the long-term target.
BIP_CLIENT_TOKEN=replace-with-a-long-random-token

# Optional voice overrides for local testing only.
# Leave blank unless you intentionally map characters to custom voice IDs.
RAYLENE_VOICE_ID=
RYLANE_VOICE_ID=
CLOUD_VOICE_ID=
NIGHT_VOICE_ID=
SEKRET_VOICE_ID=
PARENT_COACH_VOICE_ID=

# Supabase safety-scan secrets do NOT go in .dev.vars for the Worker.
# Set those on Supabase instead:
#   supabase secrets set SAFETY_SCAN_SECRET=...
#   supabase secrets set SUPABASE_SERVICE_ROLE_KEY=...
#   supabase secrets set OPENAI_API_KEY=...
