name = "sekret-backend"
main = "worker/observed-index.ts"
compatibility_date = "2026-06-13"
compatibility_flags = ["nodejs_compat"]

[observability]
enabled = true

# Non-secret model vars — safe to commit.
# To rotate a model: edit here and run `wrangler deploy --name sekret-backend`.
# Secrets (OPENAI_API_KEY) must use `wrangler secret put`, never this file.
[vars]
OPENAI_CHAT_MODEL = "gpt-4o"
OPENAI_TTS_MODEL  = "gpt-4o-mini-tts"
OPENAI_STT_MODEL  = "whisper-1"

# Bridge summary rollout control (worker/bridge-summary.ts). Unset = enabled
# for everyone. Set to "disabled" as a hard kill switch, or to a
# comma-separated list of teen user IDs for a controlled beta cohort — either
# takes effect on the next deploy, no app release required.
# BRIDGE_SUMMARIES_ROLLOUT = "disabled"
