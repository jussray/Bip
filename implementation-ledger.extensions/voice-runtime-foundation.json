{
  "id": "voice-runtime-foundation",
  "name": "Privacy-safe shared voice runtime telemetry foundation",
  "status": "contract",
  "ownerIssue": "https://github.com/jussray/Sekret-Bip/issues/460",
  "acceptanceCriteria": [
    "One ordered migration defines voice_sessions, voice_turns, voice_events, and voice_latency_metrics with RLS enabled.",
    "Telemetry writes are server-owned; permanent authenticated owners may read their own metadata and delete a top-level session tree.",
    "Anon has no table or sequence privileges, anonymous-authenticated sessions fail closed, and cross-user access is denied.",
    "Raw audio, transcript text, prompts, replies, and private message content are absent from the schema.",
    "Event payloads are bounded metadata and reject obvious raw-content keys, including nested keys.",
    "No voice Storage bucket exists until uploader, retention, deletion, isolation, MIME, size, and rollback contracts are approved together.",
    "Static contracts and a rollback-contained live authorization probe exist before production migration approval."
  ],
  "contractPaths": [
    "docs/VOICE_RUNTIME_FOUNDATION.md",
    "supabase/migrations/20260717034535_create_voice_runtime_foundation.sql"
  ],
  "testPaths": [
    "test/voice-runtime-foundation.test.mjs",
    "supabase/probes/voice_runtime_foundation.sql",
    "test/implementation-ledger.test.mjs"
  ],
  "verification": {
    "state": "partial",
    "evidence": "https://github.com/jussray/Sekret-Bip/issues/460",
    "blocker": "Run exact-head repository gates, apply the migration only under separate founder approval, then execute rollback-contained owner/cross-user/anonymous/payload/cascade proof and Supabase advisors before promotion."
  },
  "rollout": {
    "state": "disabled",
    "control": "No client or server runtime consumes these tables in Phase 1."
  },
  "rollback": "Revert the repository migration before application. After any approved live application, use a separately reviewed rollback migration only after confirming no runtime consumer or retained evidence depends on the tables."
}
