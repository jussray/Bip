# Social Provisioning Security Boundary

The lab is a local founder-only rehearsal surface. It has no password fields, no token storage, no OAuth callback, no account-creation endpoint, no external browser automation, and no API authority over social platforms. Any future connector must be introduced separately through official OAuth and platform permissions.
