/**
 * src/types/index.ts
 *
 * Type barrel — re-exports from legacy types/ directory.
 * Physical .ts files move here in Step 3.
 *
 * Usage: import type { VoiceNote } from '@/types';
 */
export type * from '../../types/index';
