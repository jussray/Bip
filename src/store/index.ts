/**
 * src/store/index.ts
 *
 * Global state barrel.
 * Re-exports existing stores while migration is in progress.
 *
 * Usage: import { useAppStore } from '@/store';
 */
export { useAppStore }   from '../../src/store/useAppStore';
