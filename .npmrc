# .npmrc
# legacy-peer-deps=true resolves React 18/19 peer dep conflicts across
# the Expo SDK 56 + react-native 0.79 ecosystem.
# Matches the --legacy-peer-deps flag used in vercel.json installCommand.
legacy-peer-deps=true
strict-peer-deps=false
