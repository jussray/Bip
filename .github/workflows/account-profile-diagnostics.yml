name: Account Profile Diagnostics

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test-output:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - name: Capture type-check and test output
        shell: bash
        run: |
          set +e
          npm run type-check > typecheck-output.txt 2>&1
          type_status=$?
          npm test > test-output.txt 2>&1
          test_status=$?
          cat typecheck-output.txt
          cat test-output.txt
          echo "$type_status" > typecheck-exit-code.txt
          echo "$test_status" > test-exit-code.txt
          exit 0
      - name: Upload diagnostics
        uses: actions/upload-artifact@v4
        with:
          name: account-profile-test-output
          path: |
            typecheck-output.txt
            typecheck-exit-code.txt
            test-output.txt
            test-exit-code.txt
          if-no-files-found: error
